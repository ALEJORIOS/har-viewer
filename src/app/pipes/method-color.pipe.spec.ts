import { MethodColorPipe } from './method-color.pipe';

describe('MethodColorPipe', () => {
  it('create an instance', () => {
    const pipe = new MethodColorPipe();
    expect(pipe).toBeTruthy();
  });
});
